# heroes-server
BackHeroes, aqui encontraras el archivo del backend del HeroesApp para su utilización con el frontend.

## Primero se necesita instalar json-server
se ejecuta este comando, esta es la version compatible:
npm install -g json-server@0.17.4

## Busca la carpeta del back 
con Cd y la ruta

## Ejecuta el json-server
con el siguiente comando para empezar a usarlo:
json-server --watch db.json 
